<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yq.train.mapper.CourseExtMapper">
  <resultMap id="BaseResultMap" type="com.yq.train.model.Course">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Nov 04 18:46:47 CST 2019.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="teaching_id" jdbcType="INTEGER" property="teachingId" />
    <result column="describe" jdbcType="VARCHAR" property="describe" />
    <result column="price" jdbcType="INTEGER" property="price" />
    <result column="student_count" jdbcType="INTEGER" property="studentCount" />
    <result column="gmt_create" jdbcType="DATE" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="DATE" property="gmtModified" />
    <result column="headportrait_url" jdbcType="VARCHAR" property="headportraitUrl" />
  </resultMap>
  <select id="countBySearch" parameterType="com.yq.train.dto.CourseQueryDTO" resultType="java.lang.Integer">
    select count(*)  from course
    <where>
      <if test="search != null">
        and course_describe regexp #{search}
      </if>
    </where>
  </select>
  <select id="selectBySearch" parameterType="com.yq.train.dto.CourseQueryDTO" resultMap="BaseResultMap">
    select * from course
    <where>
      <if test="search != null">
        and course_describe regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>

  <select id="teacherCourseCountBySearch" parameterType="com.yq.train.dto.TeacherCourseDTO" resultType="java.lang.Integer">
    select count(*)  from course
    <where>
      <if test="search != null">
        and course_describe regexp #{search}
      </if>
      and teaching_id = #{teachingId}
    </where>

  </select>
  <select id="selectByTeacherCourseSearch" parameterType="com.yq.train.dto.TeacherCourseDTO" resultMap="BaseResultMap">
    select * from course
    <where>
      <if test="search != null">
        and course_describe regexp #{search}
      </if>
      and teaching_id = #{teachingId}
    </where>
    order by gmt_create desc limit #{page},#{size}
  </select>

</mapper>